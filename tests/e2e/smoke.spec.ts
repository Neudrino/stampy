/**
 * Phase 0 placeholder E2E test.
 *
 * This keeps the Playwright suite non-empty without requiring a running
 * WordPress site. Real end-to-end tests are added in later phases.
 */

import { test, expect } from '@playwright/test';

test( 'phase 0 placeholder', async () => {
	expect( 1 + 1 ).toBe( 2 );
} );
